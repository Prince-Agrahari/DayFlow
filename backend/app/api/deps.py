"""Shared API dependencies (auth, database sessions).

Implementation pending — feature/backend-hrms branch.
"""

# Placeholder for:
# - get_db() — SQLAlchemy session
# - get_current_user() — JWT validation
# - require_role(role) — RBAC decorator
